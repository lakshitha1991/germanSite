{% extends '::indexLayout.html.twig' %}

{% block pannelTitle %}
    CruiseLine
{% endblock %}
{% block content_table %}
    <table class="table table-striped table-bordered table-condensed table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cruiseline</th>
                <td>{{ entity.cruiseLine }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
{% block other %}
    <ul class="record_actions">
    <li>
        <a href="{{ path('cruiseline') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cruiseline_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
