{% extends '::indexLayout.html.twig' %}

{% block pannelTitle %}
    CruiseLine list
{% endblock %}
{% block content_table %}
    <table class="table table-striped table-bordered table-condensed table-responsive">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cruiseline</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cruiseline_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cruiseLine }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cruiseline_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cruiseline_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
{% block other %}
    <ul>
        <li>
            <a href="{{ path('cruiseline_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
